# 测试场地说明

本目录用于验证 Obsidian 配置同步 MVP，作为可重复使用的固定测试数据。

## Vault 角色

- `vault1`：主库，提供待同步的配置文件和目录。
- `vault2`：最小目标库，用于测试新增配置和已有文件覆盖。
- `vault3`：混合目标库，用于测试文件覆盖、目录递归复制以及目标库额外文件保留。

## 使用方式

测试时选择 `vault1` 作为主库，选择 `vault2`、`vault3` 作为从库，然后按同步范围生成计划并执行同步。

执行前应保留本目录中的基准内容；如果同步直接修改了这些目录，应先恢复基准内容再开始下一轮测试。
